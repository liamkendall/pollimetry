pollimetry Function

Wrap-all #function, 
#1. select Taxa (Check)
#2. Select Family (Check) NA = ALL.
#2. select Data type (BL, BLxW, HW, un-transformed) (Check)
#3. Long/Lat provided (Check), calculate distance between eq and samples and rank by distance,
    #####Tolerate just latitude (Martin et al. 2014 coefficients for body length)
#4. Optional: check if values within range of equation sample

##individual functions for 

#Indivdual equations DONE >>

#to family equations >>
dipBL
#If group is given - hierarchial eg. return ALL + Nemataocera but not non-nematoceran 



#to taxa equations

##Check all mm